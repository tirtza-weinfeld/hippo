# Database Configuration
# PostgreSQL connection string (Neon with branch support recommended)
# Format: postgresql+psycopg://user:password@host:port/database?sslmode=require
#
# RECOMMENDED SETUP:
# - Local development: Use Neon 'development' branch
# - Production (Railway): Use Neon 'production' branch
#
# How to set up:
# 1. Get development branch URL from Neon dashboard (click on 'development' branch)
# 2. Paste it below for local development
# 3. Set production branch URL in Railway environment variables only
#
DATABASE_URL=postgresql+psycopg://user:password@ep-your-dev-endpoint.neon.tech/neondb?sslmode=require&channel_binding=require

# Hugging Face Hub Configuration
# Repository name for storing trained models (format: username/repo-name)
HF_MODEL_REPO=your-username/your-model-repo

# Name of the default model to load from HuggingFace Hub
DEFAULT_MODEL=your-model-name

# Optional: Hugging Face API token (required for private repos)
# Get token from: https://huggingface.co/settings/tokens
HUGGINGFACE_TOKEN=hf_your_token_here

# CORS Origins (comma-separated list of allowed origins)
# Example: https://yourapp.vercel.app,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000

# Optional: Custom Hugging Face cache directory
# HF_CACHE_DIR=.hf_cache

# Optional: Comet ML for experiment tracking
# Get API key from: https://www.comet.com/api/my/settings/
# COMET_API_KEY=your_comet_api_key

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
